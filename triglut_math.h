
#include "trig_luts.h"

#pragma once

/** Fast sine function using a lookup table. */
float lut_fastsin(double angle);

/** Fast cosine function using a lookup table. */
float lut_fastcos(double angle);
